document.querySelector(invoice - num.invoice - num).innerText =
    document.querySelector(invoice - num.date).innerText =
    document.querySelector(invoice - num.invoice - num).innerText =
    document.querySelector(invoice - num.invoice - num).innerText =
    document.querySelector(invoice - num.invoice - num).innerText =
    document.querySelector(invoice - num.invoice - num).innerText =
    document.querySelector(invoice - num.invoice - num).innerText =
    document.querySelector(invoice - num.text - success).innerText =